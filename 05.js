const myArray = [
    { name: 'Puspa', age: 22 },
    { name: 'Wulan', age: 20 },
    { name: 'Putri', age: 18 }
  ];
  
  // loop through the array and print each object
  for (let i = 0; i < myArray.length; i++) {
    console.log(`Name: ${myArray[i].name}, Age: ${myArray[i].age}`);
  }